{{- if .Values.rbac.enable }}
# permissions for end users to edit chronicles.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: chronicle-editor-role
rules:
- apiGroups:
  - core.posit.team
  resources:
  - chronicles
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - core.posit.team
  resources:
  - chronicles/status
  verbs:
  - get
{{- end -}}
