---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: postgresdatabases.core.posit.team
spec:
  group: core.posit.team
  names:
    kind: PostgresDatabase
    listKind: PostgresDatabaseList
    plural: postgresdatabases
    shortNames:
    - pgdb
    - pgdbs
    singular: postgresdatabase
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: PostgresDatabase is the Schema for the postgresdatabases API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PostgresDatabaseSpec defines the desired state of PostgresDatabase
            properties:
              extensions:
                items:
                  type: string
                type: array
              mainDbCredentialSecret:
                description: MainDatabaseCredentialSecret configures the secret used
                  for storing the main database credentials
                properties:
                  type:
                    type: string
                  vaultName:
                    type: string
                type: object
              schemas:
                items:
                  type: string
                type: array
              secret:
                description: Secret is configuration to use for retrieving the password
                properties:
                  type:
                    type: string
                  vaultName:
                    type: string
                type: object
              secretPasswordKey:
                description: SecretPasswordKey is the password key to use (within
                  the SecretVault)
                type: string
              secretVault:
                description: SecretVault is the secretId to use for retrieving the
                  password
                type: string
              teardown:
                properties:
                  drop:
                    type: boolean
                type: object
              url:
                pattern: ^postgres.+@.+/.+
                type: string
              workloadSecret:
                description: WorkloadSecret configures the managed persistent secret
                  for the entire workload account
                properties:
                  type:
                    type: string
                  vaultName:
                    type: string
                type: object
            required:
            - secretPasswordKey
            - secretVault
            - url
            type: object
          status:
            description: PostgresDatabaseStatus defines the observed state of PostgresDatabase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
